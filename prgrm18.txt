#include <stdio.h>

int main() {
    int date, month, year;
    printf("enter your DOB:");
    scanf("%d %d %d",&date, &month, &year);
    printf("DOB is %d/%d/%d", date, month, year);
    

    return 0;
}